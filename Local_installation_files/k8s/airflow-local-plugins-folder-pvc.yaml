apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: airflow-local-plugins-folder
  namespace: airflow
spec:
  storageClassName: hostpath
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi 